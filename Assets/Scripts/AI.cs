using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AI : MonoBehaviour
{

    // Distance in +45, 0, and -45 degrees.
    float distance1;
    float distance2;
    float distance3;

    // Difference between the midpoint of the next two poles and the current position in the Y Axis.
    float differenceY;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
